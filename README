Run script modules to generate data output for use on the wiki.

```
python -m script.<script_file_name>
```

##Asset Bundles

Use AssetStudioMod to export **MonoBehaviour**, **MonoScript**, **GameObject**, **TextAsset**, and **Transform** from the following:

```
designer_config
scene/additive/*
storyscript
resourcepoint
```

###Text

From `localization/english`, export:

```
*MonoBehaviour AssetItemEnglish
*MonoScript    AssetItem
```

###Images & Icons

And **Texture2D** and **Sprite** from:

```
uisystem_sprite
```

After each asset export, also export the assets list with Export -> Asset list to XML -> Filtered assets.

##Credit

This is [PermDenied's code](https://github.com/cruiseliu/sandrock-scripts); I'm just learning it so I can update the Lua modules. Mostly that complicated `ItemSource` one.
